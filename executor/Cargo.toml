
[package]
name = "executor"
version = "0.1.0"
edition = "2021"

[dependencies]
fyrox = { workspace = true }
chessgame = { path = "../game", optional = true }

[features]
default = ["chessgame"]
dylib = ["fyrox/dylib"]
